# 中国幸福生活计算器 — 详细实施计划

## 一、总体目标

实现一个全量功能的中国幸福生活计算器，包括以下功能模块：
- 两种模式：按现状推算 / 按目标推算
- 两种收入类型：有薪资 / 无薪资
- 生活等级系统（L1-L5）
- 年龄-资金曲线模拟
- 调整建议生成（增加收入、减少支出、补充本金、增加被动收入、降低生活等级、组合方案）
- 方案保存与对比
- 分享脱敏结果
- 滑杆实时模拟
- 风险等级评估
- 全量页面及导航：主页、设置页、结果页、保存列表页、方案对比页、分享页、帮助中心

目标：首版上线即包含流程图上所有功能，前端用户体验流畅、界面现代、美观，计算逻辑精确可靠。

---

## 二、技术栈

- 前端：Next.js 15 + React + TypeScript + Tailwind CSS
- 表单和校验：React Hook Form + Zod
- 状态管理：Zustand 或 React Context
- 图表：Recharts
- 数据库（可选）：PostgreSQL / SQLite
- 后端 API：Next.js API Routes
- 数据存储：本地 localStorage + 登录用户保存到数据库

---

## 三、实施阶段及任务拆解

### 阶段 1：基础架构与数据准备
**目标**：搭建项目结构、类型定义、城市数据与生活等级基础数据。

#### 任务
1. 初始化 Next.js 15 项目，配置 TypeScript 和 Tailwind CSS。
2. 设计目录结构：`components`、`lib`、`data`、`types`、`pages`。
3. 定义 TypeScript 类型：CalculatorMode、IncomeType、LivingLevel、ExpenseBreakdown、CalculatorFormState、CalculatorResult 等。
4. 准备城市数据（城市名称、城市等级、基准月支出、各项指数）。
5. 准备生活等级数据（L1-L5、支出倍率、描述）。
6. 建立表单校验 schema (Zod) 。

**输出**：完整项目基础目录，类型和数据定义完成。

---

### 阶段 2：计算引擎开发
**目标**：实现全量计算逻辑，确保四种场景可计算。

#### 任务
1. 编写年度模拟函数 `simulateYearly`。
2. 实现四种场景计算函数：
   - 按现状推算 + 有薪资收入
   - 按现状推算 + 无薪资收入
   - 按目标推算 + 有薪资收入
   - 按目标推算 + 无薪资收入
3. 实现生活等级计算与可负担等级判断。
4. 实现风险等级评估函数。
5. 实现调整建议生成函数（含二分搜索求最小增加收入、减少支出、补充本金等）。
6. 实现目标模式可达性判断及最终余额计算。
7. 写单元测试覆盖所有函数。

**输出**：计算引擎模块完成，可独立返回 CalculatorResult。

---

### 阶段 3：页面设计与实现
**目标**：实现全量页面布局与交互。风格与主页统一。

#### 页面清单与任务
1. **主页（Landing Page）**
   - 产品介绍、模式入口、收入类型说明、生活等级预览、功能亮点、CTA按钮。
2. **模式选择 / 设置页（Setup Page）**
   - 模式选择卡片
   - 收入类型选择卡片
   - 基础信息表单 + 高级设置
   - 表单校验和动态字段显示
3. **结果页（Result Page）**
   - 核心结论卡片
   - 年龄-资金曲线
   - 当前 vs 目标生活等级对比
   - 调整建议卡片
   - 滑杆模拟
   - 保存方案、分享操作
4. **方案保存列表页（Saved Plans Page）**
   - 方案卡片网格、搜索与筛选
   - 新建 / 编辑 / 删除 / 复制 / 加入对比
   - 概览侧边栏与分页
5. **方案对比页（Plan Comparison Page）**
   - 选中方案 chips
   - 对比 summary 卡片
   - 对比表格
   - 资金曲线对比图表与最终余额风险对比图表
   - 系统推荐卡片与操作按钮
6. **分享结果页（Share Page）**
   - 脱敏结果显示（可维持年龄、生活等级、风险等级）
   - 建议摘要卡片
   - 简化趋势图
   - 分享操作按钮
7. **辅助页面（FAQ / Privacy / About / Terms）**
   - 左侧导航，右侧内容区域
   - 常见问题手风琴
   - 隐私说明 / 产品介绍 / 使用条款
   - 支持 CTA banner

**输出**：各页面初版高保真 UI 完成，布局、风格、组件可交互。

---

### 阶段 4：前端交互与状态管理
**目标**：实现滑杆模拟、表单动态更新、保存方案、方案对比、分享功能。

#### 任务
1. 状态管理：全局 store 保存表单状态、计算结果、方案列表。
2. 滑杆模拟：绑定输入值，实时计算并更新结果。
3. 保存方案功能：localStorage + 后端 API。
4. 方案对比功能：选择方案、生成对比表格与图表。
5. 分享功能：生成脱敏 SharedResult 对象，提供复制 / 海报 / 再测算按钮。
6. 响应式设计：保证页面在常用桌面分辨率可用。

**输出**：页面交互逻辑完成，用户操作流畅。

---

### 阶段 5：后端 API 与数据库（可选）
**目标**：提供持久化存储、方案管理与分享功能。

#### 任务
1. 数据库设计：
   - cities 表
   - calculator_plans 表
   - shared_results 表
2. API 路由实现：
   - /api/cities
   - /api/calculator/calculate（可选，前端计算可不依赖）
   - /api/calculator/plans (CRUD)
   - /api/calculator/compare
   - /api/calculator/share
3. 登录用户方案保存接口与权限校验。

**输出**：后端接口可用，支持保存、对比、分享。

---

### 阶段 6：测试与验收
**目标**：确保功能正确，体验流畅。

#### 测试点
1. **计算测试**：四种场景、调整建议、风险等级、目标可达性。
2. **页面测试**：布局、响应式、滑杆实时刷新、按钮功能。
3. **方案管理测试**：保存、删除、复制、加入对比。
4. **分享测试**：脱敏展示、分享链接有效。
5. **辅助页面测试**：FAQ手风琴、隐私说明、条款显示。
6. **性能测试**：计算引擎运行时间，图表渲染速度。

**输出**：所有功能通过 QA 验收，用户体验无阻。

---

### 阶段 7：上线与迭代
1. 部署到正式环境
2. 收集用户反馈，修复潜在问题
3. 优化滑杆性能、图表交互、页面加载速度
4. 后续迭代：增加多城市比较、历史方案分析、导出功能等

---

## 四、实施时间表（参考）

| 阶段 | 时间 | 里程碑 |
|---|---|---|
| 1. 基础架构与数据准备 | 1 周 | 项目初始化，类型和数据完成 |
| 2. 计算引擎开发 | 2 周 | 四种场景计算函数和建议生成完成，单元测试覆盖 |
| 3. 页面设计与实现 | 3 周 | 高保真页面布局完成，组件可交互 |
| 4. 前端交互与状态管理 | 2 周 | 滑杆、方案保存、对比、分享功能完成 |
| 5. 后端 API 与数据库 | 1 周 | 保存、对比、分享接口完成 |
| 6. 测试与验收 | 1 周 | 功能、交互、性能 QA 完成 |
| 7. 上线与迭代 | 持续 | 部署上线，收集反馈优化 |

总计约 **10 周** 可完成首版全量功能实施。

---

## 五、关键风险与防控
1. **计算性能问题**：复杂年度模拟可能卡顿 → 使用缓存、批量计算、节流滑杆操作。
2. **数据输入错误**：用户填写异常值 → 表单校验 + 提示信息。
3. **页面复杂度过高**：过多表单和图表 → 分步骤引导，分列布局。
4. **脱敏分享泄露风险**：分享结果需去掉敏感信息 → 共享对象仅包含生活等级、可维持年龄、风险等级。
5. **多方案对比显示**：图表拥挤 → 限制对比数量、缩放图表、提供滚动或分页。

---

## 六、总结
此实施计划覆盖 PRD 中 **全部功能**：计算引擎、页面、交互、方案管理、分享、风险提示。按阶段、任务和时间表细化，可直接进入开发执行。

